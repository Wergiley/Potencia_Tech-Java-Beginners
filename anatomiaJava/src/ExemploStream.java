public class ExemploStream {
   // Classe Anônima, é uma classe que não recebe nome,
   // é declarado e instanciado em uma única instrução.
   
   /*
    Ex: 
    x.sort(new Comparator<X>() {
        @Override
        public int compare(X x1, X x2) {
        return Integer.comapare(x1.getX(), x2.getX());
    }
    }); 
   */ 

  // Lambda, é uma função sem declaração, não necessita de nome, 
  // retorno e modificador de acesso, ex: (argumento) -> (corpo)

  
}
